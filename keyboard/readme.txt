The files in this directory have the following uses:

tofu60-layout.json - KLE layout data
tofu60-design.json - above layout data wrapped in a way via can interpret
tofu60-map.json    - mapping of switches to keys
og60.json          - the original unaltered design file from KBDfans
